# AGENTS.md

## Project
Weather Monitor Japan

日本国内の現在の気象状況を、地図を中心にリアルタイムに近い形で常時表示するWebアプリを開発する。

## Development principles

- 一度に大きく作らず、1タスクにつき1つの明確な完了条件を扱う。
- 実装前に `docs/REQUIREMENTS.md` と `docs/ARCHITECTURE.md` を読む。
- 仕様と実装が食い違う場合は、実装を勝手に広げず、仕様側の更新が必要か判断する。
- 将来機能を先回りして実装しない。
- UIコンポーネントに気象庁側のURL、レスポンス形式、タイル仕様などを直接埋め込まない。
- 外部データはアダプター層を通し、アプリ内部で扱う形式へ変換する。
- 取得失敗、データ欠損、更新遅延を前提として設計する。
- 失敗時に古いデータを即座に消さず、最終成功時刻とデータ時刻を表示できるようにする。
- 過剰な依存関係を避ける。目的を満たす最小構成を優先する。
- 必要のない認証、データベース、バックエンド常駐処理は導入しない。
- UIはデスクトップで常時表示して使えることを優先しつつ、狭い画面でも破綻しないようにする。
- アクセシビリティを無視しない。主要操作はクリック、タップ、キーボードで利用可能にする。

## Coding rules

- TypeScriptを優先する。
- 型定義を外部レスポンスへ直接依存させない。
- データ取得、正規化、表示を分離する。
- マジックナンバーや更新間隔は設定値として管理する。
- コンポーネントは表示責務を優先し、データ取得ロジックを肥大化させない。
- エラーは黙って握りつぶさない。
- 変更に応じてREADMEまたはdocsを更新する。

## Task workflow

1. リポジトリ全体を確認する。
2. 今回のタスクに関係する仕様を確認する。
3. 実装範囲と完了条件を整理する。
4. 必要最小限の変更を行う。
5. lint、typecheck、test、buildのうち利用可能なものを実行する。
6. 動作確認結果と残課題を報告する。

## Out of scope unless explicitly requested

- アカウント機能
- ログイン
- 課金
- プッシュ通知
- ネイティブアプリ
- 独自気象予測
- AIによる予報生成
- 長期履歴データベース
- SNS機能
- 多言語化
