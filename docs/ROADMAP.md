# ROADMAP.md

## Development policy

各バージョンは1つの主要機能に集中する。

前段階が安定する前に次段階を先行実装しない。

## v0.1 Rain Monitor

Goal:

日本地図上で現在および直近過去の降水情報を確認できる。

Done when:

- 地図が表示される。
- ズームとパンができる。
- 降水レイヤーが表示される。
- レイヤーをON/OFFできる。
- 表示データ時刻が分かる。
- 一定間隔で更新される。
- 複数時刻を切り替えられる。
- 取得失敗時に直前データが残る。

## v0.2 AMeDAS

Goal:

地図上で主要な地上観測値を確認できる。

Status:

`codex/v0.2-amedas` で実装。

Candidate data:

- 気温
- 降水量
- 風向
- 風速

Done when:

- 全国の観測地点を地図に表示できる。
- 気温、1時間降水量、風向・風速を確認できる。
- 表示する観測要素を切り替えられる。
- 観測時刻と更新状態を確認できる。
- 取得失敗時に直前の観測値が残る。

## v0.3 Himawari

Goal:

衛星画像から日本周辺の雲の状態を確認できる。

## v0.4 Timeline Player

Goal:

過去データを時間軸で連続再生できる。

Status:

`claude/v0.4-timeline` で実装。対象は雨雲のみ。

Features:

- 再生
- 一時停止
- 時刻選択
- 再生速度

Done when:

- 保持している雨雲の時刻を連続再生できる。
- 再生と一時停止を切り替えられる。
- 再生速度を選べる。
- 画像の読み込みが終わってから次の時刻へ進む。
- 最新時刻まで進んだあと先頭へ戻る。
- 時刻を手動で選ぶと再生が止まる。

Out of scope:

- 衛星画像とアメダスの連続再生。必要性を確認してからv0.5以降で扱う。

## v0.5 Precipitation Forecast

Goal:

現在から未来の降水予測まで連続して確認できる。

## v0.6 Alerts

Goal:

気象警報、注意報を地図上で把握できる。

## v0.7 Advanced Weather Layers

Candidate features:

- 雷
- 台風
- 天気図
- 風
- 気温分布

## Later

必要性を確認してから検討する。

- PWA
- 全画面常時表示モード
- 複数画面レイアウト
- 地域プリセット
- ユーザー設定保存
- 気象データ履歴
- デスクトップアプリ化
