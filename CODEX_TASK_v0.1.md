# Codex Task: v0.1 Rain Monitor

このリポジトリでは、日本国内のリアルタイム気象情報を地図上で表示するWebアプリを開発します。

最初に以下を読んでください。

- `AGENTS.md`
- `docs/REQUIREMENTS.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`

今回の実現したいことは「日本地図上に降水情報を表示できる最小構成を作ること」です。

実装範囲はv0.1に限定してください。将来機能を先回りして実装しないでください。

## 完了条件

- ブラウザで日本地図が表示される。
- 地図をズーム、パンできる。
- 降水情報を地図上のレイヤーとして表示できる。
- 降水レイヤーをON/OFFできる。
- 現在表示している気象データの時刻が確認できる。
- 一定間隔で新しいデータを確認できる。
- 複数の過去時刻を切り替えられる。
- データ取得失敗時も最後に正常取得した表示を維持する。
- README.mdに起動方法を追記する。
- 利用可能なlint、typecheck、test、buildを実行する。

## 制約

- 気象データの取得処理はUIから分離する。
- 外部データ仕様をそのままUIコンポーネントへ露出させない。
- 必要性がない限りバックエンドを追加しない。
- 必要性がない限り状態管理ライブラリを追加しない。
- v0.2以降の機能は実装しない。

## 作業手順

作業開始前にリポジトリを確認し、今回変更するファイルと実装方針を簡潔に示してください。

その後、実装を進めてください。

完了後は以下を報告してください。

- 変更した内容
- 動作確認結果
- テスト結果
- 残っている問題
- 次に着手すべき最小タスク
