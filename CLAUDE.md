# CLAUDE.md

このリポジトリの開発規約は `AGENTS.md` に集約する。Claude Code もそれに従う。

@AGENTS.md

## 補足

- 実装前に `docs/REQUIREMENTS.md`、`docs/ARCHITECTURE.md`、`docs/ROADMAP.md` を読む。
- バージョンごとの検証結果は `docs/VALIDATION_v0.x.md` に残す。
- 検証コマンド: `npm run lint`、`npm run typecheck`、`npm test`、`npm run build`、`npm run test:e2e`。
- 作業ブランチは `<agent>/<内容>` 形式（例: `codex/v0.2-amedas`、`claude/v0.4-timeline`）。
